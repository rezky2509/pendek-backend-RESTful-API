import {User} from '../../models/Users'
// Middleware 
// Setter the value that is allowed
export type ApplicationVariables = {
    user: User
    // Here defined the properties the the type
    // kiki: string
}